1、
函数内，var声明的变量是局部变量，可以进行变量提升，提升作用只声明不赋值，返回undefined
函数内，变量和函数都声明了，函数提升高于变量提升
2、
类是具有相同属性和方法的对象集合
类是抽象的
对象是具体 实例化
3、
对象存储在堆里，堆内存大空间私有，因此c语言程序员要手动清理堆内存，否则会造成堆溢出，也有自动清理内存
栈内存：存储局部变量、函数参数线性结构  filo   在函数调用结束后消失
4、
//能识别
typeof NaN-----number
typeof function(){}-------function(引用)
//不能区别
typeof null-----object（基本）
typeof {}--------object
typeof []----------object

a instanceof b a是不是b的实例


箭头函数不能new：没有prototype和自己的this指向，不可使用argument参数
one：没有自己的this，只会在自己作用于的上一层继承this，所以箭头函数在定义是已经确定了，之后不会改变


